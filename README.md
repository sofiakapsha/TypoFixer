###### Практична робота №3

#### Важливо! 

Ця практична робота передбачає роботу у **парі**. Очікується, що ви самостійно сформуєте пари, розділите роботу між собою і прийдете на захист разом. 

## Можливо, ви мали на увазі..?

### Мета роботи

Попрактикуватись в задачах, що вирішуються методом динамічного програмування; реалізувати просту програму виправлення помилок у написанні слів.

## Що будемо робити?

Ми всі звикли, що фактично будь-яке текстове поле для вводу, чи то цілий текстовий документ в редакторі, чи просто створення нового посту в інстаграмі супроводжується перевіркою граматики - неправильні слова підкреслюються та надаються рекомендації по виправленню.

![Spell Example](./Examples/spell.png)

В цій роботі ми створимо просту систему, що буде здатна знаходити та виправляти помилки у написанні слів.

## Порядок виконання

### Підготовка

0. Прочитайте 9 розділ підручника Grokking Algorithms, щоб освіжити знання про динамічне програмування, особливо частину про знаходження найбільшого спільного підрядка та послідовності.
1. Скачайте [підготовлений список англійських слів](./Examples/words_list.txt). Це один з так званих SCOWL - [Spell Checker Oriented Word Lists](http://wordlist.aspell.net/), тобто спеціально підготовлених списків слів саме для використання в якості бази для перевірки написання.
2. Напишіть просту консольну програму, що зчитує у користувача довільне речення (все до натискання клавіші `Enter`). Розбийте речення на слова, виключіть пунктуаційні знаки, та перевірте кожне слово на входження у список. Для кожного слова, яке ви не знайшли, виведіть підказку на екран. Наприклад:

    ```C#
    > Togay a have the greatest tahk!
    < Looks like you have typos in next words: 'Togay', 'tahk'
    ```

### Пошук підказок

Тепер ми готові не просто підсвітити слова, яких немає в словнику, а спробувати знайти підказки для виправлення. Почнемо з простого:

1. Реалізуйте розглянутий на лекції алгоритм [Longest common subsequence](https://en.wikipedia.org/wiki/Longest_common_subsequence_problem). Пам'ятайте - код копіпастити не можна! Прочитайте алгоритм, розберіть його ще раз та реалізуйте самостійно. Для кожного "невідомого" слова знайдіть 5 найкращих кандидатів та виводіть їх користувачу. Спробуйте підібрати приклади, коли використання цього алгоритму призведе до некоректних підказок.
2. Гарним способом знайти можливі варіанти виправлення є пошук edit distance двох рядків. За визначенням, це кількість модифікуючих операцій які необхідно зробити над одним рядком, щоб отримати інший.

Підвидом, який ми будемо використовувати, є відстань Левенштейна, яка враховує операції вставки, заміни або видалення символів. Наприклад, відстань Левенштейна між рядками crate та flat - 4:`crate` -> `cate` (видалення `r`), `cate` -> `cat` (видалення `e`), `cat` -> `fat` (заміна c на f), `fat` -> `flat` (вставка l). Для знаходження відстані Левенштейна зазвичай використовується динамічне програмування.

Детальне пояснення концепції відстані Левенштейна та сам алгоритм [можна прочитати тут](https://medium.com/@ethannam/understanding-the-levenshtein-distance-equation-for-beginners-c4285a5604f0). Модифікуйте програму, так щоб виводити підказки використовуючи алгоритм Левенштейна.

Існує певна модифікації алгоритму, що дозволяє враховувати окрім наведених операцій (вставка, заміна, видалення), ще й перестановку місцями літер. Це дуже корисно для виправлення помилок, оскільки часто випадково виходить написати щось типу "Приівт!" замість "Привіт!". Для того щоб отримати повний бал за цю роботу знайдіть цю модифікацію та реалізуйте.

### Автодоповнення

Сучасні редактори часто пропонують доповнити наступне слово, якщо ви вже достатньо написали. Наприклад, при введенні "informa" редактор може показати продовження у вигляді "tion" тьмяним кольором, а при натисканні TAB слово доповниться і ви продовжите писати після нього. Було б круто реалізувати такий самий функціонал і у вашому редакторі - можна почати без "показує тьмяним кольором", але щоб доповнення мало сенс. При цьому відстань Левенштейна вам не дуже підходить - ви хочете шукати слова з конкретним префіксом, а не найближчі по редагуванню. Наприклад, коли вводите `crois`, у якості підказки ви хочете отримати `croissant` (хоч відстань редагування аж 4), а не `crock` (відстань 2). 

Тут є безліч варіантів, як реалізувати потрібну вам логіку, серед яких:
- Бінарне дерево пошуку зі всіма словами
- Відсортований список + бінарний пошук (його в рамках цього курсу ми не розглядали, але ви цілком можете самостійно ознайомитися)
- Префіксне дерево (Trie) - знову-таки, у рамках курсу не розглядали, але ви так само можете ознайомитися самостійно
- Словник (Dictionary) з префіксами у якості ключів і словами у якост значень.
- І ще кілька інших... 

Вибирайте той варіант, який потім зможете пояснити на захисті 🙃


### Інтерфейс користувача 

У сучасних редакторах вам не потрібно дописувати весь текст, щоб отримати підсвічування по типу "можливо, ви мали на увазі...". Так само до слів, підсвічених як помилкові, можна повернутися і виправити їх на запропоновані "по місцю" (без необхідності самостійно передруковувати текст). Та і інтерфейс до функціоналу "автодоповнення", який описано вище, теж часто реалізовано трохи розумніше, ніж просто "доповнювати по TAB".

У консольних додатках теж можна зробити гарненький інтерфейс (завдяки бібліотекам по типу curses). Додаткове завдання полягає у тому, щоб продумати і реалізувати зручний інтерфейс саме для редагування і виправлення тексту. 


## Контрольні питання

- В чому суть методики динамічного програмування? Наведіть приклади відомих вам алгоритмів
- Які переваги має динамічне програмування перед рекурсією?
- Що таке відстань Левенштейна та як її порахувати?


## Оцінювання

Максимальний бал - 13 (+ 4 додаткових):

- Підготовча частина - _2 бали_;
- Реалізація алгоритму Левенштейна - _3 бали_;
- Реалізація варіації алгоритму Левенштейна, що враховує перестановки - _2 бал_;
- Теоретичні питання - _6 балів_;

**Додаткові завдання**
- Реалізація окремого режиму "автодоповнення" (див. розділ "Автодоповнення") (по натисненню TAB) - _2 бали_
- Реалізація "зручної версії" інтерфейсу користувача через curses або подібні бібліотеки (див. розділ "Інтерфейс користувача") - _до +2 балів_

## Захист

Зверніть увагу, що на теоретичні питання відведено _6 балів_. Незалежно від того, хто яку частину практичної роботи виконував, від обох членів команди очікується повне розуміння теорії. При цьому у вас можуть як запитати теоретичну частину (включно з "контрольними питаннями", але не обмежуючись ними), так і попросити на листочку, наприклад, руками побудувати ДП по відстані Левенштейна.

Додаткові теми, які можуть фігурувати у питаннях:
- Пошук найбільшої спільної підпослідовності
- Пошук найбільшої зростаючої підпослідовності
- Задача про рюкзак
- Хешування